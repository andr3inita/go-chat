define("ChatUI", ["reactjs"], function(React) {
  return React.createComponent({
    render: function() {
      return (
        React.createElement("h1", null, "Hello, World!")
      );
    }
  });
});